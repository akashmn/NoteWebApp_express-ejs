<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-900 min-h-screen p-6">
    <div class="w-1/4 max-w-4xl mx-auto">
        <!-- Go to Home Button -->
        <div class="mb-6">
            <a href="/" class="inline-flex items-center gap-2 bg-zinc-800 hover:bg-zinc-700 text-white font-medium py-2 px-4 rounded-lg transition-all duration-200 border border-zinc-700 hover:border-zinc-600 shadow-lg hover:shadow-xl">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/>
                </svg>
                Back to Home
            </a>
        </div>

                    <form 
                action="/edit"
                method="POST"
                class="space-y-6 bg-zinc-800 p-8 rounded-2xl shadow-2xl border border-zinc-700">
                <div class="space-y-2">
                    <input 
                        value="<%= title %>"
                        name="previous"
                        type="text" 
                        placeholder="old title..."
                        class="w-full px-4 py-3 bg-zinc-700 border border-zinc-600 rounded-lg text-zinc-500 placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all duration-200 hover:bg-zinc-650"
                    >

                    <input 
                        name="new"
                        type="text" 
                        placeholder="new title..."
                        class="w-full px-4 py-3 bg-zinc-700 border border-zinc-600 rounded-lg text-white placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all duration-200 hover:bg-zinc-650"
                    >
                </div>
                
                <button 
                    href="/edit"
                    type="submit"
                    class="w-full bg-gradient-to-r from-yellow-600 to-orange-600 hover:from-blue-700 hover:to-purple-700 text-white font-semibold py-3 px-6 rounded-lg transition-all duration-200 transform hover:scale-[1.02] focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 focus:ring-offset-zinc-800 shadow-lg"
                >
                    Update Name
                </button>
            </form>
    </div>
</body>
</html>