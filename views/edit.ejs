<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Task</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-900 min-h-screen p-6">
    <div class="w-full max-w-2xl mx-auto">
        <!-- Go to Home Button -->
        <div class="mb-6">
            <a href="/" class="inline-flex items-center gap-2 bg-zinc-800 hover:bg-zinc-700 text-white font-medium py-2 px-4 rounded-lg transition-all duration-200 border border-zinc-700 hover:border-zinc-600 shadow-lg hover:shadow-xl">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/>
                </svg>
                Back to Home
            </a>
        </div>

        <div class="mb-8 text-center">
            <h1 class="text-3xl font-bold text-white mb-2">Edit Task</h1>
            <p class="text-zinc-400">Update your task details</p>
        </div>

        <form 
            action="/edit"
            method="POST"
            class="space-y-6 bg-zinc-800 p-8 rounded-2xl shadow-2xl border border-zinc-700">
            
            <!-- Hidden field to store original filename -->
            <input type="hidden" name="previous" value="<%= title %>">
            
            <div class="space-y-2">
                <label class="text-sm font-medium text-zinc-300 block">Task Title (Filename)</label>
                <input 
                    value="<%= title %>"
                    name="new"
                    type="text" 
                    placeholder="Enter new filename..."
                    class="w-full px-4 py-3 bg-zinc-700 border border-zinc-600 rounded-lg text-white placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all duration-200 hover:bg-zinc-650"
                >
                <p class="text-xs text-zinc-500">Leave unchanged if you don't want to rename the file</p>
            </div>

            <div class="space-y-2">
                <label class="text-sm font-medium text-zinc-300 block">Task Description</label>
                <textarea 
                    name="description"
                    placeholder="Enter task description..."
                    rows="6"
                    class="w-full px-4 py-3 bg-zinc-700 border border-zinc-600 rounded-lg text-white placeholder-zinc-400 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all duration-200 hover:bg-zinc-650 resize-none"
                ><%= description %></textarea>
            </div>
            
            <div class="flex gap-4">
                <button 
                    type="submit"
                    class="flex-1 bg-gradient-to-r from-green-600 to-emerald-600 hover:from-green-700 hover:to-emerald-700 text-white font-semibold py-3 px-6 rounded-lg transition-all duration-200 transform hover:scale-[1.02] focus:outline-none focus:ring-2 focus:ring-green-500 focus:ring-offset-2 focus:ring-offset-zinc-800 shadow-lg"
                >
                    Update Task
                </button>
                <a 
                    href="/"
                    class="flex-1 bg-zinc-600 hover:bg-zinc-700 text-white font-semibold py-3 px-6 rounded-lg transition-all duration-200 transform hover:scale-[1.02] focus:outline-none focus:ring-2 focus:ring-zinc-500 focus:ring-offset-2 focus:ring-offset-zinc-800 shadow-lg text-center"
                >
                    Cancel
                </a>
            </div>
        </form>
    </div>
</body>
</html>