<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-900 min-h-screen p-6">
    <div class="w-full max-w-4xl mx-auto">
        <!-- Go to Home Button -->
        <div class="mb-6">
            <a href="/" class="inline-flex items-center gap-2 bg-zinc-800 hover:bg-zinc-700 text-white font-medium py-2 px-4 rounded-lg transition-all duration-200 border border-zinc-700 hover:border-zinc-600 shadow-lg hover:shadow-xl">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/>
                </svg>
                Back to Home
            </a>
        </div>
        
        <div class="mb-8 text-center bg-zinc-800 p-8 rounded-2xl shadow-2xl border border-zinc-700">
            <h1 class="text-3xl font-bold text-white mb-2"><%= title %></h1>
            <p class="text-zinc-400">Here are the details of your task</p>
        </div>
        
        <!-- Task Details Section -->
        <div class="bg-zinc-800 p-8 rounded-2xl shadow-2xl border border-zinc-700 space-y-6">
            <div class="space-y-4">
                <div class="border-l-4 border-blue-500 pl-4">
                    <h2 class="text-xl font-semibold text-white mb-2">Task Description</h2>
                    <p class="text-zinc-300 leading-relaxed">
                        <%= description %>
                </div>
            </div>
        </div>
    </div>
</body>
</html>